{{#each rounds as |round|}}

  <ul data-id="{{round.id}}">
  <li><h3>Title: {{book.title}}</h3></li>
  <li>
    {{#if book.description}}
      {{book.description}}
    {{else}}
      No description.
    {{/if}}
  </li>
  <li><button class="removeBook">Remove</button></li>
  </ul>

{{/each}}
